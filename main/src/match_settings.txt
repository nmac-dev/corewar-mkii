## Match Setting Values
#   | core_size       | number of memory addresses within the core
#   | max_cycles      | max number of cycles beofre the round has been concluded
#   | max_processes   | max number of processes a single warrior can create
#   | max_warrior_len | max length of a warriors instructions (max lines)
#   | min_separation  | min distance between warriors at the start of a round
##

## Parameters
core_size = 8000
max_cycles = 8
max_processes = 8
max_warrior_len = 12
min_separation = 8